version: '3.9'
services:
  api:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - .:/app
    environment:
      - MODEL_PATH=models/best_model.joblib
      - FEATURE_PATH=models/feature_columns.json
